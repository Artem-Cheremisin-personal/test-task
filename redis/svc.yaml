apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: redis
spec:
  clusterIP: None # None to make it a headless service
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: redis